[package]
name = "envview-iced"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.14.0", default-features = false, features = ["tiny-skia", "crisp", "web-colors", "thread-pool", "linux-theme-detection", "x11", "wayland"] }
